# Superseded production / art / asset docs (archived 2026-05-08)

These 40 docs were collapsed into a single drag-and-drop production document at
`docs/ART_DEPARTMENT_PRODUCTION.md`.

Nothing here is deleted — every file is preserved at its archived path with full
git history. The unified doc has a "Doc archive ledger" section in §7 that maps
each archived path back to the section that replaced it.

If you need historical context for any of these (a deprecated naming convention,
a previous prompt revision, a snapshot of which assets had shipped on a given
date), read the file in place. Do not promote files back out of this folder —
update `ART_DEPARTMENT_PRODUCTION.md` instead.
